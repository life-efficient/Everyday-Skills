interface:
  display_name: "Fanout Tasks"
  short_description: "Show copyable TODO prompt blocks in chat."
  default_prompt: "Use $fanout-tasks to show up to 10 ready TODO prompt blocks in chat, plus tasks needing more specification."
